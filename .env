# PostgreSQL Configuration for messege_api
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=root
DB_NAME=messege_api
DB_PORT=5432

# Comma separated list of allowed origins for CORS.
# Use "*" to allow all (matches the original PHP behavior).
ALLOWED_ORIGINS=*
